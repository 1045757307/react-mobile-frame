import { Input, Button } from 'antd-mobile';

const Login = () => {
  return (
    <div>
      <Input />
      <Button block color="primary">
        登录
      </Button>
    </div>
  );
};
export default Login;
